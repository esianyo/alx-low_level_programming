#include <stdio.h>
#include <stdlib.h>
/**
 * main - Print single digits
 *
 * Return: Always 0
 */
int main(void)
{
	int single;

	for (single = 0; single < 10; single++)
		printf(single);
	printf('\n');
	return (0);
}
